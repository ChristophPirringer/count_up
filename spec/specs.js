describe('countUp', function() {

  it("takes the target_number of 10 and the step_number of 2 and returns the array of [2,4,6,8,10]", function() {
   expect(countUp(10,2)).to.equal([2, 4, 6, 8, 10]);
  });

});
